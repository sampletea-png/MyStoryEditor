# 08: 损坏包拒绝打开

**What to build:** 作者选中无法读取的损坏数据包时，应用拒绝打开并说明原因，不把坏包写回，也不覆盖旁路已有恢复点。

**Blocked by:** 04 旁路恢复点与 Backup API

**Status:** ready-for-agent

- [ ] 截断库、缺 WAL 的拷贝、坏 `work.json` 三种夹具均拒绝打开
- [ ] 拒绝时不写回该包，不覆盖其旁已有恢复点，也不自动删除损坏包
- [ ] 提示说明损坏，而不是空白崩溃或打开成空作品
